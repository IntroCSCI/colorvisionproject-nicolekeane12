//Author: Nicole Keane
#include <iostream>
#include <fstream>
using namespace std;

int main()
{
string input;
double color;
size_t position;
string filename;
fstream reader;

reader.open (filename,ios::in);
if(reader.is_open()){
position = filename.find(",");
for (int counter= position+3;counter < input.size(); counter++) {
if( color >= (0.5);
cout << " This color is unlikely to be probelmatic based on transparency."}
else{
  cout <<" This color is likely to be probelmatic based on transparency." }









  return 0;
}
